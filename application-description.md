# 表单项目描述

我构建了一个名为 NewsPilot Agent 的 AI 新闻情报自动化项目，核心痛点是解决个人开发者和小团队每天需要反复打开多个新闻站、技术社区、产品博客和安全公告页面，手动筛选信息、判断价值、整理摘要所带来的时间浪费和信息遗漏问题。传统 RSS 或收藏夹只能聚合链接，无法判断重复报道、提取正文重点、分析行业影响，也无法把分散的信息沉淀成可复用的日报和后台记录。因此这个项目的目标是用 Agent 工作流把“发现新闻、理解新闻、合并重复内容、生成可行动摘要”完整自动化。

项目采用多 Agent 协作流程，而不是单次模型调用。采集 Agent 会根据订阅源、关键词和行业标签抓取新闻列表，并记录来源、发布时间和原始链接；解析 Agent 负责打开页面、提取正文、清理广告导航和无效段落，并把失败页面送入重试队列；去重 Agent 会根据标题、正文语义、发布时间和来源可信度合并重复报道，避免日报里出现多条同质信息；分析 Agent 会判断新闻主题、影响范围、可信度、风险等级和优先级，输出结构化标签；写作 Agent 最后根据前面产生的结构化结果生成短摘要、深度解读和每日情报简报。

我把这个项目做成了可以直接打开试用的 Web Demo，而不是只停留在概念说明。前台落地页展示项目定位、核心指标和实时 Agent 运行过程；注册入口可以让审核人员进入模拟后台；后台管理页包含任务队列、Agent 状态、处理数量、失败重试、日报结果、订阅源设置和 Token 消耗看板。审核人员打开项目地址后，可以直观看到完整功能闭环：从新闻源配置，到任务执行，再到摘要结果和消耗统计。

这个项目的 Token 消耗较高，因为一次完整任务会经过采集、解析、去重、分析、写作和复核多个阶段。尤其是正文解析、语义去重、影响分析和日报生成都需要处理较长上下文，新闻源数量增加后会产生持续、真实、可验证的模型调用需求。Demo 中展示了累计 756,420,880 tokens 的消耗看板，用来说明该项目适合长期测试 API 的上下文理解、批量处理、多 Agent 调度和成本控制能力。

后续我计划继续增强真实数据接入、失败重试策略、行业订阅、Markdown/邮件/飞书导出，以及更细粒度的质量评分机制。这个项目适合用 MiMo API 作为底层模型进行真实 Agent 场景验证，也能持续产生高频、多步骤、长上下文的模型调用需求。
